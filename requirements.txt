# Python Backend Dependencies (for future FastAPI + WebSockets implementation)
# Currently NOT USED - Frontend is Angular with mock data and localStorage only
# This file ensures team uses same versions when backend is implemented

# ============================================================================
# Core Framework
# ============================================================================
fastapi==0.104.1              # Modern async web framework
uvicorn[standard]==0.24.0     # ASGI server with WebSocket support

# ============================================================================
# Real-time Communication (WebSockets)
# ============================================================================
websockets==12.0              # WebSocket protocol implementation
python-socketio==5.10.0       # Socket.IO for real-time bidirectional communication
python-engineio==4.8.0        # Engine.IO protocol

# ============================================================================
# Data Validation & Serialization
# ============================================================================
pydantic==2.5.0               # Data validation using Python type hints
pydantic-settings==2.1.0      # Settings management

# ============================================================================
# Authentication & Security
# ============================================================================
python-jose[cryptography]==3.3.0  # JWT token creation and validation
passlib[bcrypt]==1.7.4            # Password hashing with bcrypt
python-multipart==0.0.6           # Form data parsing

# ============================================================================
# Database (for future persistent storage)
# ============================================================================
# Option 1: SQL Database
sqlalchemy==2.0.23            # SQL ORM
alembic==1.13.0               # Database migrations
asyncpg==0.29.0               # Async PostgreSQL driver
# psycopg2-binary==2.9.9      # Sync PostgreSQL driver (alternative)

# Option 2: NoSQL Database (for chat messages)
# motor==3.3.2                # Async MongoDB driver
# pymongo==4.6.0              # Sync MongoDB driver
# redis==5.0.1                # Redis for caching and real-time features

# ============================================================================
# Environment & Configuration
# ============================================================================
python-dotenv==1.0.0          # Environment variable management

# ============================================================================
# CORS & Middleware
# ============================================================================
# (Built into FastAPI, no additional package needed)

# ============================================================================
# Development & Testing Tools
# ============================================================================
black==23.12.0                # Code formatter
pytest==7.4.3                 # Testing framework
pytest-asyncio==0.21.1        # Async testing support
httpx==0.25.2                 # Async HTTP client for testing
faker==20.1.0                 # Generate fake data for testing

# ============================================================================
# Production & Monitoring (Optional)
# ============================================================================
# gunicorn==21.2.0            # Production WSGI server
# prometheus-client==0.19.0   # Metrics and monitoring
# sentry-sdk==1.39.1          # Error tracking

# ============================================================================
# Installation Instructions
# ============================================================================
# 1. Create virtual environment:
#    python -m venv venv
#
# 2. Activate virtual environment:
#    - macOS/Linux: source venv/bin/activate
#    - Windows: venv\Scripts\activate
#
# 3. Install dependencies:
#    pip install -r requirements.txt
#
# 4. For development only (minimal):
#    pip install fastapi uvicorn python-jose passlib python-dotenv
#
# ============================================================================
# Future Backend Features (when implementing)
# ============================================================================
# - WebSocket connections for real-time messaging
# - JWT authentication (replace mock auth)
# - Database storage (replace localStorage)
#   * PostgreSQL for users, conversations metadata
#   * MongoDB or Redis for messages (better for chat history)
# - Message delivery status tracking
# - Online/offline user status
# - Typing indicators
# - File uploads (images, documents)
# - Push notifications
# - Message search and indexing
# - Group chat support
# - End-to-end encryption (future)
#
# ============================================================================
# Notes
# ============================================================================
# - This file is checked in to ensure version consistency
# - Update versions periodically for security patches
# - Pin exact versions (==) for reproducible builds
# - Use 'pip freeze > requirements.txt' after adding new packages
# - Keep commented packages for optional features

